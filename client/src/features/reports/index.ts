/**
 * Reports Feature
 *
 * Generates insights and analytics on system usage, engagement, and alumni activity.
 * Access: Admin
 * Rendering: Server
 * Data: Read-only
 *
 * @example
 * ```tsx
 * <ReportsDashboard user={user} />
 * ```
 */

export type { SessionUser } from "@/types";
export { ReportsDashboard } from "./feature";
